specVersion: "v1"
serviceId: "porfoliooo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "porfoliooo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
